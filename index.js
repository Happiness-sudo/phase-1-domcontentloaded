document.addEventListener("DOMContentLoaded", function () {
  // Step A: find the paragraph by its ID
  const paragraph = document.getElementById("text");

  // Step B: change its text
  paragraph.textContent = "This is really cool!";
});
